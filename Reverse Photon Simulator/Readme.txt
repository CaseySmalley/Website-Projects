This project requires the most recent version of Firefox or Chrome and hardware that supports OpenGL ES 3.0

Native OpenGL must enabled (by disabling ANGLE)

In firefox set "webgl.prefer-native-gl" to true in about:config

In chrome, modify a shortcut to start with the flags "--use-angle=gl"
